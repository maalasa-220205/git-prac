# Industry Level Git Commands Practice

---

## 1. git config --global user.name

**Syntax:**
git config --global user.name "Your Name"

**Purpose:**
Sets the global Git username.

**Example:**
git config --global user.name "Maalasa Yarramsetti"

**Screenshot Proof:**
(Add screenshot here)
